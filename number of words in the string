#include<stdio.h>
int main()
{
	char str[100];
	int count=0,i;
	printf("enter the string:\n");
	gets(str);
	for(i=0;str[i]!='\0';i++)
	{
		if(str[i]==' ')
		{
			count++;
			
		}
	}
	printf("%d",count+1);
}
